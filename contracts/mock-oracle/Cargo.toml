[package]
name = "mock-oracle"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }
reflector-adapter = { path = "../../crates/reflector-adapter" }

[features]
testutils = ["soroban-sdk/testutils"]
